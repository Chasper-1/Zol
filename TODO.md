## Сейчас

- Доделать хоткеи (не все клавиши работают)
- Привязать режимы (Preview/LivePreview/Source) к клавишам:
  - В Preview работают стрелки, но не редактирование
  - Esc → Source или Preview (надо определить логику)
  - Tab → цикл режимов (уже есть, но проверить)
  - PageUp/PageDown (скролл без движения курсора?)
- Ctrl+C, Ctrl+V, Ctrl+X — копировать/вставить/вырезать (нужен clipboard)
- Ctrl+Z, Ctrl+Y — undo/redo (нужна история изменений)
- Ctrl+A — select all (нужна модель выделения)

## Потом

- Реестр ручек (actions/handles) — общая система: `HashMap<имя, fn(вход)>`
  - RON-слой (статика): theme, tab_width, font_size → на старте разворачиваются в ручки
  - Rhai-слой (динамика): хоткеи, макросы, кастомные команды
  - Плагины: изолированная копия реестра, не трогает ядро напрямую
- Конфиг: Rhai vs RON (пока не решили)

## Никогда (не писать)

- Маркет плагинов, песочница, версионирование API — дохера рано
