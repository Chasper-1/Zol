// Этот файл предназначен для будущей реализации управления системными шрифтами.
// Пока что он не используется в основной логике приложения.

pub struct FontRegistry {
    // В будущем здесь будет храниться список доступных системных шрифтов
    // и пути к их файлам для lazy-loading.
}

impl FontRegistry {
    pub fn new() -> Self {
        Self {}
    }
}