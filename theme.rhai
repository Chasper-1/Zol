#{
    editor: #{
        padding: 20.0,
        radius: 25.0, // Измени это значение, чтобы увидеть как закругляется окно!
        background: "#1e1e2e", // Цвет подгрузится прямо в Cairo context
    },
    text: #{
        size: 14.0,
        line_height: 22.0,
        color: "#cdd6f4",
    }
}